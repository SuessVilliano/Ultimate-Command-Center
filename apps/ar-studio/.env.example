# Server-only. Never prefix this with VITE_ or commit the real key.
SQR_API_KEY=
# Populate after scan.elevate.co is verified in SQR. IDs come from SQR API/dashboard.
SQR_DOMAIN_ID=
SQR_PROJECT_ID=
AR_API_PORT=8787
AR_STORE_PATH=.ar-studio-data.json

# Browser-safe public deployment URL for the AR viewer, HTTPS only.
VITE_PUBLIC_AR_BASE_URL=https://ar.elevate.co/
