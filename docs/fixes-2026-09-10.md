# UI and Conversations Fixes — 2026-09-10

This branch fixes three operator-visible issues reported from command.liv8.co:

1. Light theme text contrast on dark command surfaces.
2. The META SV right rail collapses to a compact side tab and releases main-content width.
3. HighLevel conversations can fall back to conversation search/last-message data when the message-export scope is unavailable, while preserving full export when permitted.

HighLevel production requirements: the deployed PIT must belong to the target sub-account/location and include conversation/message read access. Full conversation history requires message-read/export access; conversation-search-only access can still surface recent thread summaries.
